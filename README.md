# Criando makefile com CMake

O comando a seguir força a criação de uma `Makefile`

```bash
cmake  -G "Unix Makefiles" .
```

# Compilando binários

Compile os binários com make através do seguinte comando

```bash
make
```
